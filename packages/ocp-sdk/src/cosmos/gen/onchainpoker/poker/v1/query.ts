// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: onchainpoker/poker/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Table } from "./poker.js";

export const protobufPackage = "onchainpoker.poker.v1";

export interface QueryTableRequest {
  tableId: string;
}

export interface QueryTableResponse {
  table: Table | undefined;
}

export interface QueryTablesRequest {
}

export interface QueryTablesResponse {
  tableIds: string[];
}

function createBaseQueryTableRequest(): QueryTableRequest {
  return { tableId: "0" };
}

export const QueryTableRequest: MessageFns<QueryTableRequest> = {
  encode(message: QueryTableRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tableId !== "0") {
      writer.uint32(8).uint64(message.tableId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryTableRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryTableRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tableId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryTableRequest {
    return {
      tableId: isSet(object.tableId)
        ? globalThis.String(object.tableId)
        : isSet(object.table_id)
        ? globalThis.String(object.table_id)
        : "0",
    };
  },

  toJSON(message: QueryTableRequest): unknown {
    const obj: any = {};
    if (message.tableId !== "0") {
      obj.tableId = message.tableId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryTableRequest>, I>>(base?: I): QueryTableRequest {
    return QueryTableRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryTableRequest>, I>>(object: I): QueryTableRequest {
    const message = createBaseQueryTableRequest();
    message.tableId = object.tableId ?? "0";
    return message;
  },
};

function createBaseQueryTableResponse(): QueryTableResponse {
  return { table: undefined };
}

export const QueryTableResponse: MessageFns<QueryTableResponse> = {
  encode(message: QueryTableResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.table !== undefined) {
      Table.encode(message.table, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryTableResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryTableResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.table = Table.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryTableResponse {
    return { table: isSet(object.table) ? Table.fromJSON(object.table) : undefined };
  },

  toJSON(message: QueryTableResponse): unknown {
    const obj: any = {};
    if (message.table !== undefined) {
      obj.table = Table.toJSON(message.table);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryTableResponse>, I>>(base?: I): QueryTableResponse {
    return QueryTableResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryTableResponse>, I>>(object: I): QueryTableResponse {
    const message = createBaseQueryTableResponse();
    message.table = (object.table !== undefined && object.table !== null) ? Table.fromPartial(object.table) : undefined;
    return message;
  },
};

function createBaseQueryTablesRequest(): QueryTablesRequest {
  return {};
}

export const QueryTablesRequest: MessageFns<QueryTablesRequest> = {
  encode(_: QueryTablesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryTablesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryTablesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryTablesRequest {
    return {};
  },

  toJSON(_: QueryTablesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryTablesRequest>, I>>(base?: I): QueryTablesRequest {
    return QueryTablesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryTablesRequest>, I>>(_: I): QueryTablesRequest {
    const message = createBaseQueryTablesRequest();
    return message;
  },
};

function createBaseQueryTablesResponse(): QueryTablesResponse {
  return { tableIds: [] };
}

export const QueryTablesResponse: MessageFns<QueryTablesResponse> = {
  encode(message: QueryTablesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.tableIds) {
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryTablesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryTablesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.tableIds.push(reader.uint64().toString());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.tableIds.push(reader.uint64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryTablesResponse {
    return {
      tableIds: globalThis.Array.isArray(object?.tableIds)
        ? object.tableIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.table_ids)
        ? object.table_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: QueryTablesResponse): unknown {
    const obj: any = {};
    if (message.tableIds?.length) {
      obj.tableIds = message.tableIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryTablesResponse>, I>>(base?: I): QueryTablesResponse {
    return QueryTablesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryTablesResponse>, I>>(object: I): QueryTablesResponse {
    const message = createBaseQueryTablesResponse();
    message.tableIds = object.tableIds?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
