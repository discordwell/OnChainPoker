// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: onchainpoker/poker/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "onchainpoker.poker.v1";

export interface MsgCreateTable {
  creator: string;
  smallBlind: string;
  bigBlind: string;
  minBuyIn: string;
  maxBuyIn: string;
  actionTimeoutSecs: string;
  dealerTimeoutSecs: string;
  playerBond: string;
  rakeBps: number;
  /** default 9 */
  maxPlayers: number;
  label: string;
}

export interface MsgCreateTableResponse {
  tableId: string;
}

export interface MsgSit {
  player: string;
  tableId: string;
  /** 0..8 */
  seat: number;
  buyIn: string;
  /** 32-byte ristretto point (required for dealer mode) */
  pkPlayer: Uint8Array;
}

export interface MsgSitResponse {
}

export interface MsgStartHand {
  caller: string;
  tableId: string;
}

export interface MsgStartHandResponse {
}

export interface MsgAct {
  player: string;
  tableId: string;
  /** fold|check|call|bet|raise */
  action: string;
  /** For bet/raise: desired total street commitment ("BetTo"). */
  amount: string;
}

export interface MsgActResponse {
}

export interface MsgTick {
  caller: string;
  tableId: string;
}

export interface MsgTickResponse {
}

export interface MsgLeave {
  player: string;
  tableId: string;
}

export interface MsgLeaveResponse {
}

function createBaseMsgCreateTable(): MsgCreateTable {
  return {
    creator: "",
    smallBlind: "0",
    bigBlind: "0",
    minBuyIn: "0",
    maxBuyIn: "0",
    actionTimeoutSecs: "0",
    dealerTimeoutSecs: "0",
    playerBond: "0",
    rakeBps: 0,
    maxPlayers: 0,
    label: "",
  };
}

export const MsgCreateTable: MessageFns<MsgCreateTable> = {
  encode(message: MsgCreateTable, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.smallBlind !== "0") {
      writer.uint32(16).uint64(message.smallBlind);
    }
    if (message.bigBlind !== "0") {
      writer.uint32(24).uint64(message.bigBlind);
    }
    if (message.minBuyIn !== "0") {
      writer.uint32(32).uint64(message.minBuyIn);
    }
    if (message.maxBuyIn !== "0") {
      writer.uint32(40).uint64(message.maxBuyIn);
    }
    if (message.actionTimeoutSecs !== "0") {
      writer.uint32(48).uint64(message.actionTimeoutSecs);
    }
    if (message.dealerTimeoutSecs !== "0") {
      writer.uint32(56).uint64(message.dealerTimeoutSecs);
    }
    if (message.playerBond !== "0") {
      writer.uint32(64).uint64(message.playerBond);
    }
    if (message.rakeBps !== 0) {
      writer.uint32(72).uint32(message.rakeBps);
    }
    if (message.maxPlayers !== 0) {
      writer.uint32(80).uint32(message.maxPlayers);
    }
    if (message.label !== "") {
      writer.uint32(90).string(message.label);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreateTable {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreateTable();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.smallBlind = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.bigBlind = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.minBuyIn = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxBuyIn = reader.uint64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.actionTimeoutSecs = reader.uint64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.dealerTimeoutSecs = reader.uint64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.playerBond = reader.uint64().toString();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.rakeBps = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.maxPlayers = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.label = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreateTable {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      smallBlind: isSet(object.smallBlind)
        ? globalThis.String(object.smallBlind)
        : isSet(object.small_blind)
        ? globalThis.String(object.small_blind)
        : "0",
      bigBlind: isSet(object.bigBlind)
        ? globalThis.String(object.bigBlind)
        : isSet(object.big_blind)
        ? globalThis.String(object.big_blind)
        : "0",
      minBuyIn: isSet(object.minBuyIn)
        ? globalThis.String(object.minBuyIn)
        : isSet(object.min_buy_in)
        ? globalThis.String(object.min_buy_in)
        : "0",
      maxBuyIn: isSet(object.maxBuyIn)
        ? globalThis.String(object.maxBuyIn)
        : isSet(object.max_buy_in)
        ? globalThis.String(object.max_buy_in)
        : "0",
      actionTimeoutSecs: isSet(object.actionTimeoutSecs)
        ? globalThis.String(object.actionTimeoutSecs)
        : isSet(object.action_timeout_secs)
        ? globalThis.String(object.action_timeout_secs)
        : "0",
      dealerTimeoutSecs: isSet(object.dealerTimeoutSecs)
        ? globalThis.String(object.dealerTimeoutSecs)
        : isSet(object.dealer_timeout_secs)
        ? globalThis.String(object.dealer_timeout_secs)
        : "0",
      playerBond: isSet(object.playerBond)
        ? globalThis.String(object.playerBond)
        : isSet(object.player_bond)
        ? globalThis.String(object.player_bond)
        : "0",
      rakeBps: isSet(object.rakeBps)
        ? globalThis.Number(object.rakeBps)
        : isSet(object.rake_bps)
        ? globalThis.Number(object.rake_bps)
        : 0,
      maxPlayers: isSet(object.maxPlayers)
        ? globalThis.Number(object.maxPlayers)
        : isSet(object.max_players)
        ? globalThis.Number(object.max_players)
        : 0,
      label: isSet(object.label) ? globalThis.String(object.label) : "",
    };
  },

  toJSON(message: MsgCreateTable): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.smallBlind !== "0") {
      obj.smallBlind = message.smallBlind;
    }
    if (message.bigBlind !== "0") {
      obj.bigBlind = message.bigBlind;
    }
    if (message.minBuyIn !== "0") {
      obj.minBuyIn = message.minBuyIn;
    }
    if (message.maxBuyIn !== "0") {
      obj.maxBuyIn = message.maxBuyIn;
    }
    if (message.actionTimeoutSecs !== "0") {
      obj.actionTimeoutSecs = message.actionTimeoutSecs;
    }
    if (message.dealerTimeoutSecs !== "0") {
      obj.dealerTimeoutSecs = message.dealerTimeoutSecs;
    }
    if (message.playerBond !== "0") {
      obj.playerBond = message.playerBond;
    }
    if (message.rakeBps !== 0) {
      obj.rakeBps = Math.round(message.rakeBps);
    }
    if (message.maxPlayers !== 0) {
      obj.maxPlayers = Math.round(message.maxPlayers);
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCreateTable>, I>>(base?: I): MsgCreateTable {
    return MsgCreateTable.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCreateTable>, I>>(object: I): MsgCreateTable {
    const message = createBaseMsgCreateTable();
    message.creator = object.creator ?? "";
    message.smallBlind = object.smallBlind ?? "0";
    message.bigBlind = object.bigBlind ?? "0";
    message.minBuyIn = object.minBuyIn ?? "0";
    message.maxBuyIn = object.maxBuyIn ?? "0";
    message.actionTimeoutSecs = object.actionTimeoutSecs ?? "0";
    message.dealerTimeoutSecs = object.dealerTimeoutSecs ?? "0";
    message.playerBond = object.playerBond ?? "0";
    message.rakeBps = object.rakeBps ?? 0;
    message.maxPlayers = object.maxPlayers ?? 0;
    message.label = object.label ?? "";
    return message;
  },
};

function createBaseMsgCreateTableResponse(): MsgCreateTableResponse {
  return { tableId: "0" };
}

export const MsgCreateTableResponse: MessageFns<MsgCreateTableResponse> = {
  encode(message: MsgCreateTableResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tableId !== "0") {
      writer.uint32(8).uint64(message.tableId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgCreateTableResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgCreateTableResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.tableId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgCreateTableResponse {
    return {
      tableId: isSet(object.tableId)
        ? globalThis.String(object.tableId)
        : isSet(object.table_id)
        ? globalThis.String(object.table_id)
        : "0",
    };
  },

  toJSON(message: MsgCreateTableResponse): unknown {
    const obj: any = {};
    if (message.tableId !== "0") {
      obj.tableId = message.tableId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgCreateTableResponse>, I>>(base?: I): MsgCreateTableResponse {
    return MsgCreateTableResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgCreateTableResponse>, I>>(object: I): MsgCreateTableResponse {
    const message = createBaseMsgCreateTableResponse();
    message.tableId = object.tableId ?? "0";
    return message;
  },
};

function createBaseMsgSit(): MsgSit {
  return { player: "", tableId: "0", seat: 0, buyIn: "0", pkPlayer: new Uint8Array(0) };
}

export const MsgSit: MessageFns<MsgSit> = {
  encode(message: MsgSit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.player !== "") {
      writer.uint32(10).string(message.player);
    }
    if (message.tableId !== "0") {
      writer.uint32(16).uint64(message.tableId);
    }
    if (message.seat !== 0) {
      writer.uint32(24).uint32(message.seat);
    }
    if (message.buyIn !== "0") {
      writer.uint32(32).uint64(message.buyIn);
    }
    if (message.pkPlayer.length !== 0) {
      writer.uint32(42).bytes(message.pkPlayer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgSit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.player = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tableId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.seat = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.buyIn = reader.uint64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pkPlayer = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgSit {
    return {
      player: isSet(object.player) ? globalThis.String(object.player) : "",
      tableId: isSet(object.tableId)
        ? globalThis.String(object.tableId)
        : isSet(object.table_id)
        ? globalThis.String(object.table_id)
        : "0",
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      buyIn: isSet(object.buyIn)
        ? globalThis.String(object.buyIn)
        : isSet(object.buy_in)
        ? globalThis.String(object.buy_in)
        : "0",
      pkPlayer: isSet(object.pkPlayer)
        ? bytesFromBase64(object.pkPlayer)
        : isSet(object.pk_player)
        ? bytesFromBase64(object.pk_player)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgSit): unknown {
    const obj: any = {};
    if (message.player !== "") {
      obj.player = message.player;
    }
    if (message.tableId !== "0") {
      obj.tableId = message.tableId;
    }
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.buyIn !== "0") {
      obj.buyIn = message.buyIn;
    }
    if (message.pkPlayer.length !== 0) {
      obj.pkPlayer = base64FromBytes(message.pkPlayer);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgSit>, I>>(base?: I): MsgSit {
    return MsgSit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgSit>, I>>(object: I): MsgSit {
    const message = createBaseMsgSit();
    message.player = object.player ?? "";
    message.tableId = object.tableId ?? "0";
    message.seat = object.seat ?? 0;
    message.buyIn = object.buyIn ?? "0";
    message.pkPlayer = object.pkPlayer ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgSitResponse(): MsgSitResponse {
  return {};
}

export const MsgSitResponse: MessageFns<MsgSitResponse> = {
  encode(_: MsgSitResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgSitResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgSitResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgSitResponse {
    return {};
  },

  toJSON(_: MsgSitResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgSitResponse>, I>>(base?: I): MsgSitResponse {
    return MsgSitResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgSitResponse>, I>>(_: I): MsgSitResponse {
    const message = createBaseMsgSitResponse();
    return message;
  },
};

function createBaseMsgStartHand(): MsgStartHand {
  return { caller: "", tableId: "0" };
}

export const MsgStartHand: MessageFns<MsgStartHand> = {
  encode(message: MsgStartHand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.caller !== "") {
      writer.uint32(10).string(message.caller);
    }
    if (message.tableId !== "0") {
      writer.uint32(16).uint64(message.tableId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgStartHand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgStartHand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.caller = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tableId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgStartHand {
    return {
      caller: isSet(object.caller) ? globalThis.String(object.caller) : "",
      tableId: isSet(object.tableId)
        ? globalThis.String(object.tableId)
        : isSet(object.table_id)
        ? globalThis.String(object.table_id)
        : "0",
    };
  },

  toJSON(message: MsgStartHand): unknown {
    const obj: any = {};
    if (message.caller !== "") {
      obj.caller = message.caller;
    }
    if (message.tableId !== "0") {
      obj.tableId = message.tableId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgStartHand>, I>>(base?: I): MsgStartHand {
    return MsgStartHand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgStartHand>, I>>(object: I): MsgStartHand {
    const message = createBaseMsgStartHand();
    message.caller = object.caller ?? "";
    message.tableId = object.tableId ?? "0";
    return message;
  },
};

function createBaseMsgStartHandResponse(): MsgStartHandResponse {
  return {};
}

export const MsgStartHandResponse: MessageFns<MsgStartHandResponse> = {
  encode(_: MsgStartHandResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgStartHandResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgStartHandResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgStartHandResponse {
    return {};
  },

  toJSON(_: MsgStartHandResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgStartHandResponse>, I>>(base?: I): MsgStartHandResponse {
    return MsgStartHandResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgStartHandResponse>, I>>(_: I): MsgStartHandResponse {
    const message = createBaseMsgStartHandResponse();
    return message;
  },
};

function createBaseMsgAct(): MsgAct {
  return { player: "", tableId: "0", action: "", amount: "0" };
}

export const MsgAct: MessageFns<MsgAct> = {
  encode(message: MsgAct, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.player !== "") {
      writer.uint32(10).string(message.player);
    }
    if (message.tableId !== "0") {
      writer.uint32(16).uint64(message.tableId);
    }
    if (message.action !== "") {
      writer.uint32(26).string(message.action);
    }
    if (message.amount !== "0") {
      writer.uint32(32).uint64(message.amount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAct {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAct();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.player = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tableId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.action = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.amount = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAct {
    return {
      player: isSet(object.player) ? globalThis.String(object.player) : "",
      tableId: isSet(object.tableId)
        ? globalThis.String(object.tableId)
        : isSet(object.table_id)
        ? globalThis.String(object.table_id)
        : "0",
      action: isSet(object.action) ? globalThis.String(object.action) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "0",
    };
  },

  toJSON(message: MsgAct): unknown {
    const obj: any = {};
    if (message.player !== "") {
      obj.player = message.player;
    }
    if (message.tableId !== "0") {
      obj.tableId = message.tableId;
    }
    if (message.action !== "") {
      obj.action = message.action;
    }
    if (message.amount !== "0") {
      obj.amount = message.amount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgAct>, I>>(base?: I): MsgAct {
    return MsgAct.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgAct>, I>>(object: I): MsgAct {
    const message = createBaseMsgAct();
    message.player = object.player ?? "";
    message.tableId = object.tableId ?? "0";
    message.action = object.action ?? "";
    message.amount = object.amount ?? "0";
    return message;
  },
};

function createBaseMsgActResponse(): MsgActResponse {
  return {};
}

export const MsgActResponse: MessageFns<MsgActResponse> = {
  encode(_: MsgActResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgActResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgActResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgActResponse {
    return {};
  },

  toJSON(_: MsgActResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgActResponse>, I>>(base?: I): MsgActResponse {
    return MsgActResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgActResponse>, I>>(_: I): MsgActResponse {
    const message = createBaseMsgActResponse();
    return message;
  },
};

function createBaseMsgTick(): MsgTick {
  return { caller: "", tableId: "0" };
}

export const MsgTick: MessageFns<MsgTick> = {
  encode(message: MsgTick, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.caller !== "") {
      writer.uint32(10).string(message.caller);
    }
    if (message.tableId !== "0") {
      writer.uint32(16).uint64(message.tableId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgTick {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgTick();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.caller = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tableId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgTick {
    return {
      caller: isSet(object.caller) ? globalThis.String(object.caller) : "",
      tableId: isSet(object.tableId)
        ? globalThis.String(object.tableId)
        : isSet(object.table_id)
        ? globalThis.String(object.table_id)
        : "0",
    };
  },

  toJSON(message: MsgTick): unknown {
    const obj: any = {};
    if (message.caller !== "") {
      obj.caller = message.caller;
    }
    if (message.tableId !== "0") {
      obj.tableId = message.tableId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgTick>, I>>(base?: I): MsgTick {
    return MsgTick.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgTick>, I>>(object: I): MsgTick {
    const message = createBaseMsgTick();
    message.caller = object.caller ?? "";
    message.tableId = object.tableId ?? "0";
    return message;
  },
};

function createBaseMsgTickResponse(): MsgTickResponse {
  return {};
}

export const MsgTickResponse: MessageFns<MsgTickResponse> = {
  encode(_: MsgTickResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgTickResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgTickResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgTickResponse {
    return {};
  },

  toJSON(_: MsgTickResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgTickResponse>, I>>(base?: I): MsgTickResponse {
    return MsgTickResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgTickResponse>, I>>(_: I): MsgTickResponse {
    const message = createBaseMsgTickResponse();
    return message;
  },
};

function createBaseMsgLeave(): MsgLeave {
  return { player: "", tableId: "0" };
}

export const MsgLeave: MessageFns<MsgLeave> = {
  encode(message: MsgLeave, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.player !== "") {
      writer.uint32(10).string(message.player);
    }
    if (message.tableId !== "0") {
      writer.uint32(16).uint64(message.tableId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgLeave {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgLeave();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.player = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tableId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgLeave {
    return {
      player: isSet(object.player) ? globalThis.String(object.player) : "",
      tableId: isSet(object.tableId)
        ? globalThis.String(object.tableId)
        : isSet(object.table_id)
        ? globalThis.String(object.table_id)
        : "0",
    };
  },

  toJSON(message: MsgLeave): unknown {
    const obj: any = {};
    if (message.player !== "") {
      obj.player = message.player;
    }
    if (message.tableId !== "0") {
      obj.tableId = message.tableId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgLeave>, I>>(base?: I): MsgLeave {
    return MsgLeave.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgLeave>, I>>(object: I): MsgLeave {
    const message = createBaseMsgLeave();
    message.player = object.player ?? "";
    message.tableId = object.tableId ?? "0";
    return message;
  },
};

function createBaseMsgLeaveResponse(): MsgLeaveResponse {
  return {};
}

export const MsgLeaveResponse: MessageFns<MsgLeaveResponse> = {
  encode(_: MsgLeaveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgLeaveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgLeaveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgLeaveResponse {
    return {};
  },

  toJSON(_: MsgLeaveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgLeaveResponse>, I>>(base?: I): MsgLeaveResponse {
    return MsgLeaveResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgLeaveResponse>, I>>(_: I): MsgLeaveResponse {
    const message = createBaseMsgLeaveResponse();
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
